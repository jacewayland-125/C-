/*Far inserire due numeri naturali, e se sono in ordine crescente
far stampare tutti i numeri compresi nell’intervallo tra i due*/

#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    const int N = 2;
    int vett[N];
    for (int i = 0; i < N; i++)
    {
        cout << "Dammi il primo valore" << endl;
        cin >> vett[1];
        cout << "Dammi il secondo valore" << endl;
        cin >> vett[2];
        if (vett[1] <= vett[2])
        {
        }
    }
}